# PDFs programmes (outils de travail, pas déployés)
data/programmes/*.pdf

# Cache Python
__pycache__/
*.pyc

# Crawl registry (cache de travail, pas déployé)
data/crawl-registry.json

# Backups
*.backup-*
